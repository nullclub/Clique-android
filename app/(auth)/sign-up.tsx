import { View, Text, TouchableOpacity } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
import { router } from "expo-router";

const signup = () => {
  return (
    <SafeAreaView>
      <Text className="text-red-500">ass</Text>
    </SafeAreaView>
  );
};

export default signup;
